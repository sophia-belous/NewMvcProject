@using NewBlog.Domain.Entities 
@model IList<Tag>

<li class="nav-header"><h2>Tags</h2></li>

    @if (Model.Count > 0)
    {
        foreach (var tag in Model)
        {
            <span class="tags">@Html.TagLink(tag)</span>
        }
    }
    else
    {
        <p>No tags found!</p>
    }


@helper Tags(System.Web.Mvc.HtmlHelper htmlHelper,
    IList<NewBlog.Domain.Entities.Tag> tags)
{
    foreach (var tag in tags)
    {
            <span class="label label-success tag">@NewBlog.WebUI.ActionLinkExtensions.TagLink(htmlHelper, tag)</span>
    }
}
